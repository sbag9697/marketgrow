# Railway Root Directory 설정 방법 (상세 가이드)

## 📍 Root Directory를 /backend로 설정하는 방법

### 방법 1: Railway Dashboard에서 설정 (권장)

#### 1단계: Railway 프로젝트 접속
```
1. https://railway.app 로그인
2. Dashboard에서 해당 프로젝트 클릭
3. 프로젝트 이름 확인 (예: marketgrow)
```

#### 2단계: Settings 탭으로 이동
```
프로젝트 페이지 상단 메뉴에서:
- Overview
- Deployments  
- Settings ← 이것을 클릭!
- Variables
- Logs
```

#### 3단계: Service Settings 찾기
```
Settings 페이지에서 스크롤 내려서:

1. "Service" 섹션 찾기
2. "Source" 카드 찾기
3. "Root Directory" 입력 필드 찾기
```

#### 4단계: Root Directory 설정
```
1. "Root Directory" 입력 필드에 클릭
2. /backend 입력 (슬래시 포함!)
3. 입력 후 자동 저장됨
4. "Applied" 또는 체크 표시 확인
```

#### 5단계: 재배포 트리거
```
설정 변경 후:
1. Deployments 탭으로 이동
2. "Redeploy" 버튼 클릭
3. 또는 GitHub에 새 커밋 푸시
```

### 방법 2: railway.json으로 설정

프로젝트 루트에 `railway.json` 파일 생성:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "root": "/backend",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### 방법 3: Railway CLI 사용

```bash
# Railway CLI 설치
npm install -g @railway/cli

# 로그인
railway login

# 프로젝트 연결
railway link

# Root Directory 설정
railway service set root /backend

# 확인
railway status
```

## ⚠️ 주의사항

### Root Directory 설정이 안 보이는 경우

1. **Service 타입 확인**
   - GitHub Repo 연결 서비스만 Root Directory 설정 가능
   - Template 기반 서비스는 불가능

2. **권한 확인**
   - 프로젝트 Owner 또는 Admin 권한 필요
   - Member는 설정 변경 불가

3. **새 서비스 생성**
   ```
   1. 현재 서비스 삭제
   2. "New" > "GitHub Repo" 선택
   3. 저장소 선택 시 Root Directory 설정 옵션 표시
   ```

## 🔍 설정 확인 방법

### Build Logs에서 확인
```
Deployments > 최신 배포 클릭 > Build Logs

다음과 같은 메시지 확인:
"Using root directory: /backend"
"Installing dependencies from /backend/package.json"
```

### 파일 구조 확인
```
빌드 로그에서:
- "Found package.json at /backend/package.json" ✅
- "Found package.json at /package.json" ❌
```

## 🚨 일반적인 오류 해결

### 1. "Cannot find package.json"
```
원인: Root Directory 설정 안됨
해결: /backend로 설정
```

### 2. "npm ERR! code ENOENT"
```
원인: 잘못된 경로
해결: 
- /backend (O)
- backend (X) - 슬래시 없음
- ./backend (X) - 점 포함
```

### 3. 설정 후에도 실패
```
1. 캐시 클리어
   - Settings > Advanced > Clear Build Cache
   
2. 강제 재배포
   - git commit --allow-empty -m "Force redeploy"
   - git push
```

## 📸 시각적 가이드

### Settings 페이지 위치
```
┌─────────────────────────────────┐
│  🚂 Railway Dashboard           │
├─────────────────────────────────┤
│  Project: marketgrow            │
├─────────────────────────────────┤
│  Overview | Deployments |       │
│  [Settings] | Variables | Logs  │
└─────────────────────────────────┘
```

### Root Directory 입력 필드
```
┌─────────────────────────────────┐
│  Service Settings               │
├─────────────────────────────────┤
│  Source                         │
│  ┌─────────────────────────┐   │
│  │ Root Directory          │   │
│  │ [/backend             ] │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

## ✅ 체크리스트

- [ ] Railway 프로젝트에 로그인했는가?
- [ ] Settings 탭을 찾았는가?
- [ ] Root Directory 필드를 찾았는가?
- [ ] /backend를 정확히 입력했는가? (슬래시 포함)
- [ ] 자동 저장이 되었는가?
- [ ] 재배포가 트리거되었는가?
- [ ] Build Logs에서 올바른 경로 확인했는가?

## 🆘 추가 도움말

만약 위 방법이 작동하지 않으면:

1. **Railway Discord/Support 문의**
   - https://discord.gg/railway
   
2. **대안: 백엔드만 별도 저장소로 분리**
   - backend 폴더를 새 저장소로 이동
   - 별도 프로젝트로 배포

3. **임시 해결책**
   - 루트에 package.json 생성
   - start 스크립트에서 backend 실행
   ```json
   {
     "scripts": {
       "start": "cd backend && npm install && npm start"
     }
   }
   ```