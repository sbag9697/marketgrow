# MarketGrow 메인 서버 설정

server {
    listen 80;
    server_name localhost marketgrow.co www.marketgrow.co;
    
    # HTTP에서 HTTPS로 리다이렉트 (프로덕션 환경)
    # return 301 https://$server_name$request_uri;
    
    # 개발 환경에서는 직접 서비스
    location / {
        try_files $uri $uri/ @app;
    }
    
    # API 프록시
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        limit_conn addr 10;
        
        proxy_pass http://app_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 로그인/회원가입 Rate Limiting
    location ~ ^/(login|register|auth) {
        limit_req zone=login burst=5 nodelay;
        try_files $uri $uri/ @app;
    }
    
    # 헬스체크 엔드포인트
    location /health {
        proxy_pass http://app_backend/health;
        proxy_set_header Host $host;
        access_log off;
    }
    
    # 정적 파일 캐싱
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary Accept-Encoding;
        
        # 정적 파일이 없으면 앱으로 프록시
        try_files $uri @app;
    }
    
    # Service Worker 캐싱 제외
    location = /sw.js {
        expires off;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri @app;
    }
    
    # 파비콘
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
    
    # robots.txt
    location = /robots.txt {
        log_not_found off;
        access_log off;
    }
    
    # 앱으로 프록시하는 백업 로케이션
    location @app {
        proxy_pass http://app_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
    
    # 보안 설정
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # 민감한 파일 접근 차단
    location ~* \.(env|log|conf)$ {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# HTTPS 설정 (SSL 인증서가 있을 때 활성화)
# server {
#     listen 443 ssl http2;
#     server_name marketgrow.co www.marketgrow.co;
#     
#     # SSL 인증서 설정
#     ssl_certificate /etc/nginx/ssl/marketgrow.co.crt;
#     ssl_certificate_key /etc/nginx/ssl/marketgrow.co.key;
#     
#     # HSTS (HTTP Strict Transport Security)
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     
#     # 나머지 설정은 HTTP 블록과 동일
#     location / {
#         try_files $uri $uri/ @app;
#     }
#     
#     location /api/ {
#         limit_req zone=api burst=20 nodelay;
#         limit_conn addr 10;
#         
#         proxy_pass http://app_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#     
#     location ~ ^/(login|register|auth) {
#         limit_req zone=login burst=5 nodelay;
#         try_files $uri $uri/ @app;
#     }
#     
#     location /health {
#         proxy_pass http://app_backend/health;
#         proxy_set_header Host $host;
#         access_log off;
#     }
#     
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#         add_header Vary Accept-Encoding;
#         try_files $uri @app;
#     }
#     
#     location = /sw.js {
#         expires off;
#         add_header Cache-Control "no-cache, no-store, must-revalidate";
#         try_files $uri @app;
#     }
#     
#     location @app {
#         proxy_pass http://app_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#     
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
#     
#     location ~* \.(env|log|conf)$ {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# }