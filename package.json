{
  "name": "marketgrow-monorepo",
  "version": "1.0.0",
  "description": "MarketGrow Full Stack Application",
  "scripts": {
    "start": "cd backend && npm install --production && node server.js",
    "install": "echo 'Skipping root install'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}